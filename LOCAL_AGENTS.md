# `hermes-agent-cascade-probe/` — Local Probe Repo (fork-local overlay)

Generated: 2026-06-10 (Wave 4 of DOX distribution; fork-local overlay)

This is a **fork-local overlay** for the `hermes-agent-cascade-probe` project repo. The repo's root `AGENTS.md` is an upstream copy of `NousResearch/hermes-agent` (58 KB) and is **upstream-governed**. Do not hand-edit it.

## Purpose

`hermes-agent-cascade-probe/` is a project repo that tracks the upstream NousResearch/hermes-agent source. It is used by Matthew to probe cascade-routing behavior in a controlled, isolated checkout. It is not the runtime Hermes Agent — that is `C:/Users/RDP/.hermes/hermes-agent/`.

## Fork-local rules

- The root `AGENTS.md` is upstream content. It is regenerated by `git pull upstream main` (or by re-applying the local patch on top of a new upstream). Do **not** hand-edit it.
- This file (`LOCAL_AGENTS.md`) is the only fork-local contract. It governs fork-local decisions, fork-local scripts, and fork-local experiments.
- Working tree dirtiness is expected and is part of the probe workflow. The probe is intentionally dirty until results are captured.
- Do not run upstream's test suite with credentials, real tokens, or real per-tenant usage in scope. The probe is a local, sandboxed checkout.

## Child DOX index

- `AGENTS.md` (root) — **upstream-governed**; read only, do not hand-edit
- `LOCAL_AGENTS.md` (this file) — fork-local probe contract (**author here**)
- `HANDOFF-*.md` — probe handoffs and evidence (state-only unless linked from here)

## DOX tree walker

- Read **this file first** for fork-local work, then upstream `AGENTS.md` for codebase structure.
- Do not add child AGENTS.md under upstream-resync paths (`agent/`, `hermes_cli/`, `tests/`, etc.).
- The local Hermes Agent runtime (`~/.hermes/hermes-agent/`) has its own fork-local overlay; this project is a probe, not the runtime.

## Approval gates

- Pulling `upstream main` is approval-gated. It will regenerate the upstream-governed root `AGENTS.md` and may introduce upstream changes that conflict with fork-local patches.
- Pushing `origin main` is approval-gated. The probe is local-only by default.
- Running the upstream test suite in this checkout is approval-gated. Use the local Hermes Agent runtime for any real test.
- Cleaning the working tree (`git clean -fd`, `git reset --hard`) is approval-gated.

## Sensitive-data rules

- This probe may load credentials from `~/.hermes/secrets/`, `~/.hermes/auth.json`, or `~/.hermes/mcp-tokens/`. It must not write to them.
- Captured probe output is state-only and never vendored. If a probe run captures a quota or token snapshot, redact before referencing it elsewhere.
- No real client data, no real tenant data, no real ShareGate AppData — these are out of scope for this probe.

## Cross-repo references

- Upstream: `https://github.com/NousResearch/hermes-agent`
- Local runtime fork: `C:/Users/RDP/.hermes/hermes-agent/` (with `LOCAL_AGENTS.md` overlay)
- Local control plane: `C:/Users/RDP/Projects/agent-os-project-packages/AGENTS.md`
- DOX tree alignment audit: `C:/Users/RDP/Projects/agent-os-project-packages/docs/agent-os/dox-tree-alignment-audit-2026-06-10.md`
